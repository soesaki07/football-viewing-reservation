parameters:
    # 解析の厳しさ（0〜9、9が最も厳しい）。
    # 学習中のプロジェクトのため、いきなり厳しいレベルにはせず、
    # 未定義の変数・メソッド・プロパティや型の不整合など基礎的な問題を検出できる
    # level 5 から開始する。慣れてきたら数字を上げて厳しくしていく。
    level: 5

    # 解析対象はapp/のみ。vendor/やdatabase/migrations、resources等は対象に含めない。
    paths:
        - app
